<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:padding="16dp"
              android:background="@android:color/background_light">

    <!-- Dropdown for Sales -->
    <Button
            android:id="@+id/buttonSales"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sales"
            android:textColor="@android:color/white"
            android:backgroundTint="#673AB7"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"
            android:padding="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

    <!-- Dropdown for Party -->
    <Button
            android:id="@+id/buttonParty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Party"
            android:textColor="@android:color/white"
            android:backgroundTint="#7B4FCA"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"
            android:padding="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

    <!-- Dropdown for Purchase -->
    <Button
            android:id="@+id/buttonPurchase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Purchase"
            android:textColor="@android:color/white"
            android:backgroundTint="#673AB7"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"
            android:padding="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

    <!-- Dropdown for Backup/Restore -->
    <Button
            android:id="@+id/buttonBackupRestore"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Backup &amp; Restore"
            android:textColor="@android:color/white"
            android:backgroundTint="#673AB7"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"
            android:padding="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

    <!-- Add Employee (No Dropdown) -->
    <Button
            android:id="@+id/buttonAddEmployee"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add Employee"
            android:textColor="@android:color/white"
            android:backgroundTint="#7B44DD"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"
            android:padding="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />
</LinearLayout>
