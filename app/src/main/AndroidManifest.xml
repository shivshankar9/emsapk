<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission is required for Google Sign-In -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Ems1"
            tools:targetApi="31">

        <!-- LoginActivity is added as the entry point of the app -->
        <activity
                android:name=".LoginActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Ems1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity is added but no longer the launcher activity -->
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Ems1"/>

        <!-- AddEmployeeActivity and AddExpenseActivity remain unchanged -->
        <activity
                android:name=".AddEmployeeActivity"
                android:label="Add Employee"
                android:theme="@style/Theme.Ems1"/>
        <activity
                android:name=".AddExpenseActivity"
                android:label="Add Expense"
                android:theme="@style/Theme.Ems1"/>

    </application>

</manifest>
